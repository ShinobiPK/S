-- ===================================================
-- DUNGEON QUEST - AUTO READY LOOP (FIXED)
-- ===================================================

local Players = game:GetService("Players")
local VirtualInputManager = game:GetService("VirtualInputManager")
local LocalPlayer = Players.LocalPlayer or Players.PlayerAdded:Wait()

local function clickButton(btn)
    if not btn then return false end
    
    local fired = false
    -- 1. ยิงผ่าน Connection
    for _, eventName in ipairs({"MouseButton1Click", "MouseButton1Down", "Activated"}) do
        if btn[eventName] then
            for _, conn in pairs(getconnections(btn[eventName])) do
                conn:Fire()
                fired = true
            end
        end
    end
    
    -- 2. จำลองคลิกเมาส์ตำแหน่งจริง
    if not fired and btn:IsA("GuiObject") and btn.AbsoluteSize.X > 0 then
        local pos = btn.AbsolutePosition
        local size = btn.AbsoluteSize
        local clickX = pos.X + (size.X / 2)
        local clickY = pos.Y + (size.Y / 2) + 36
        VirtualInputManager:SendMouseButtonEvent(clickX, clickY, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(clickX, clickY, 0, false, game, 1)
        fired = true
    end
    
    return fired
end

local function pressReadyButton()
    local playerGui = LocalPlayer:FindFirstChildOfClass("PlayerGui")
    if not playerGui then return false end

    for _, obj in ipairs(playerGui:GetDescendants()) do
        -- เช็กทั้ง TextButton, ImageButton และ Frame ที่มีปุ่มซ้อนอยู่
        if obj:IsA("TextButton") or obj:IsA("ImageButton") or obj:IsA("Button") then
            local btnName = obj.Name:lower()
            local btnText = (obj:IsA("TextButton") and obj.Text or ""):lower()
            
            -- ถ้าเจอคำว่า ready ในชื่อหรือข้อความ
            if btnName:find("ready") or btnText:find("ready") then
                clickButton(obj)
                return true
            end
        end
    end
    return false
end

-- ลูปเช็กถี่ขึ้น ทุกๆ 0.5 วินาที เพื่อให้กดทันทีที่ปุ่มเด้งขึ้นมา
task.spawn(function()
    print("🟢 [Auto Ready] เริ่มทำงานแบบเรียลไทม์แล้ว...")
    while true do
        pcall(function()
            pressReadyButton()
        end)
        task.wait(0.5)
    end
end)
